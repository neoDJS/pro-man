<%= form_with(model: project, local: true) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>      
    </div>
  <% end %>

  <div class="fields_with_errors">
    <span class="error"><%= project.errors.messages[:name].join(". ") if project.errors.any? %></span>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="fields_with_errors">
    <span class="error"><%= project.errors.messages[:description].join(". ") if project.errors.any? %></span>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="fields_with_errors">
    <span class="error"><%= project.errors.messages[:priority].join(". ") if project.errors.any? %></span>
    <%= f.label :priority %>
    <%= f.text_field :priority %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
