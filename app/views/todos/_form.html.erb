<%= form_with(model: todo, url: [todo.project, todo]) do |f| %>
  <% if todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo.errors.count, "error") %> prohibited this todo from being saved:</h2>
    </div>
  <% end %>

  <div class="fields_with_errors">
    <span class="error"><%= todo.errors.messages[:task].join(". ") if todo.errors.any? %></span>
    <%= f.label :task %>
    <%= f.text_field :task %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
